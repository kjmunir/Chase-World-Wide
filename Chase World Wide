<!DOCTYPE html>

 

<html lang="en">

<head>

  <meta charset="UTF-8" />

  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <title>[ATHLETE NAME] — Summer Travel League Fund</title>

  <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;800;900&family=Nunito:wght@400;500;600;700&display=swap" rel="stylesheet"/>

  <style>

    /* ============================================================

       CSS VARIABLES — Edit colors/fonts here

    ============================================================ */

    :root {

      --navy:     #0a1628;

      --navy-mid: #112240;

      --gold:     #f5a623;

      --gold-lt:  #ffc84a;

      --white:    #f4f1eb;

      --gray:     #8a96a8;

      --display:  'Barlow Condensed', sans-serif;

      --body:     'Nunito', sans-serif;

      /* FUNDRAISING GOAL — Edit these two numbers */

      --raised:   850;

      --goal:     2500;

    }

 

```

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

 

html { scroll-behavior: smooth; }

 

body {

  background: var(--white);

  color: var(--navy);

  font-family: var(--body);

  overflow-x: hidden;

}

 

/* ============================================================

   NAV

============================================================ */

nav {

  position: fixed; top: 0; left: 0; right: 0; z-index: 100;

  display: flex; align-items: center; justify-content: space-between;

  padding: 1rem 2.5rem;

  background: rgba(10, 22, 40, 0.92);

  backdrop-filter: blur(10px);

  border-bottom: 2px solid var(--gold);

}

 

.nav-logo {

  font-family: var(--display);

  font-weight: 900;

  font-size: 1.4rem;

  color: var(--gold);

  letter-spacing: 0.05em;

  text-decoration: none;

}

 

.nav-links { display: flex; gap: 2rem; list-style: none; }

 

.nav-links a {

  font-family: var(--display);

  font-weight: 600;

  font-size: 0.95rem;

  letter-spacing: 0.08em;

  text-transform: uppercase;

  color: var(--white);

  text-decoration: none;

  transition: color 0.2s;

}

.nav-links a:hover { color: var(--gold); }

 

.nav-donate {

  background: var(--gold);

  color: var(--navy) !important;

  padding: 0.45rem 1.2rem;

  border-radius: 4px;

  font-weight: 800 !important;

}

.nav-donate:hover { background: var(--gold-lt); color: var(--navy) !important; }

 

/* ============================================================

   HERO

============================================================ */

#hero {

  min-height: 100vh;

  display: flex; align-items: center; justify-content: center;

  position: relative;

  overflow: hidden;

  background: var(--navy);

}

 

/* Animated basketball court lines in background */

.court-bg {

  position: absolute; inset: 0;

  opacity: 0.07;

  background-image:

    radial-gradient(circle at 50% 50%, transparent 28%, rgba(245,166,35,0.8) 29%, rgba(245,166,35,0.8) 30%, transparent 31%),

    radial-gradient(circle at 50% 50%, transparent 55%, rgba(245,166,35,0.4) 56%, rgba(245,166,35,0.4) 57%, transparent 58%),

    linear-gradient(rgba(245,166,35,0.6) 1px, transparent 1px),

    linear-gradient(90deg, rgba(245,166,35,0.6) 1px, transparent 1px);

  background-size: 100% 100%, 100% 100%, 80px 80px, 80px 80px;

}

 

/* Glowing orbs */

.hero-orb {

  position: absolute;

  border-radius: 50%;

  filter: blur(80px);

  pointer-events: none;

}

.orb1 { width: 500px; height: 500px; top: -100px; right: -100px; background: rgba(245,166,35,0.15); animation: pulse 6s ease-in-out infinite; }

.orb2 { width: 300px; height: 300px; bottom: 0; left: -50px; background: rgba(17,34,64,0.8); animation: pulse 8s ease-in-out infinite reverse; }

 

@keyframes pulse {

  0%, 100% { transform: scale(1); opacity: 0.6; }

  50% { transform: scale(1.15); opacity: 1; }

}

 

.hero-content {

  position: relative; z-index: 2;

  text-align: center;

  padding: 8rem 2rem 4rem;

  max-width: 900px;

  animation: fadeUp 1s ease both;

}

 

@keyframes fadeUp {

  from { opacity: 0; transform: translateY(40px); }

  to   { opacity: 1; transform: translateY(0); }

}

 

.hero-eyebrow {

  display: inline-block;

  font-family: var(--display);

  font-weight: 700;

  font-size: 0.9rem;

  letter-spacing: 0.25em;

  text-transform: uppercase;

  color: var(--gold);

  border: 1px solid rgba(245,166,35,0.4);

  padding: 0.35rem 1rem;

  border-radius: 2px;

  margin-bottom: 1.5rem;

  animation: fadeUp 1s 0.2s ease both;

}

 

.hero-name {

  font-family: var(--display);

  font-weight: 900;

  font-size: clamp(4rem, 12vw, 9rem);

  line-height: 0.9;

  color: var(--white);

  text-transform: uppercase;

  letter-spacing: -0.01em;

  animation: fadeUp 1s 0.3s ease both;

}

 

.hero-name span { color: var(--gold); }

 

.hero-sub {

  font-family: var(--display);

  font-weight: 600;

  font-size: clamp(1rem, 2.5vw, 1.4rem);

  letter-spacing: 0.15em;

  text-transform: uppercase;

  color: var(--gray);

  margin-top: 1rem;

  animation: fadeUp 1s 0.4s ease both;

}

 

.hero-mission {

  font-size: 1.15rem;

  color: rgba(244,241,235,0.8);

  max-width: 560px;

  margin: 1.8rem auto 2.5rem;

  line-height: 1.7;

  animation: fadeUp 1s 0.5s ease both;

}

 

.btn-primary {

  display: inline-block;

  font-family: var(--display);

  font-weight: 800;

  font-size: 1.1rem;

  letter-spacing: 0.1em;

  text-transform: uppercase;

  background: var(--gold);

  color: var(--navy);

  padding: 1rem 2.8rem;

  border-radius: 4px;

  text-decoration: none;

  transition: all 0.25s;

  box-shadow: 0 0 40px rgba(245,166,35,0.3);

  animation: fadeUp 1s 0.6s ease both;

}

.btn-primary:hover {

  background: var(--gold-lt);

  transform: translateY(-3px);

  box-shadow: 0 8px 50px rgba(245,166,35,0.5);

}

 

.scroll-indicator {

  position: absolute; bottom: 2rem; left: 50%; transform: translateX(-50%);

  display: flex; flex-direction: column; align-items: center; gap: 0.5rem;

  color: var(--gray); font-size: 0.75rem; letter-spacing: 0.15em; text-transform: uppercase;

  font-family: var(--display); font-weight: 600;

  animation: fadeUp 1s 1s ease both;

}

.scroll-line {

  width: 1px; height: 50px;

  background: linear-gradient(to bottom, var(--gold), transparent);

  animation: scrollLine 2s ease-in-out infinite;

}

@keyframes scrollLine {

  0% { transform: scaleY(0); transform-origin: top; }

  50% { transform: scaleY(1); transform-origin: top; }

  51% { transform: scaleY(1); transform-origin: bottom; }

  100% { transform: scaleY(0); transform-origin: bottom; }

}

 

/* ============================================================

   SHARED SECTION STYLES

============================================================ */

section { padding: 6rem 2rem; }

 

.section-label {

  font-family: var(--display);

  font-weight: 700;

  font-size: 0.8rem;

  letter-spacing: 0.3em;

  text-transform: uppercase;

  color: var(--gold);

  margin-bottom: 0.75rem;

}

 

.section-title {

  font-family: var(--display);

  font-weight: 900;

  font-size: clamp(2.2rem, 5vw, 3.5rem);

  line-height: 1;
